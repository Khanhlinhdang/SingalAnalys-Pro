# RF Spectrum Analyzer - Requirements
# Python packages required for the RF Spectrum Analyzer application

# Core GUI Framework
PySide6>=6.5.0
PyQtGraph>=0.13.0

# Scientific Computing
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0

# Signal Processing Libraries
# Note: These may require additional system dependencies
# pyspectrum>=0.1.0  # FFT and spectrum analysis - install if available
# mhostetter/sdr - Install from GitHub: pip install git+https://github.com/mhostetter/sdr.git
scikit-dsp-comm>=0.1.0  # Digital signal processing

# Data Storage and Export
h5py>=3.7.0
pandas>=1.3.0

# Hardware Support (Optional - install only if hardware is available)
# RTL-SDR support
pyrtlsdr>=0.2.0

# HackRF support  
# pyhackrf>=0.1.0  # May need to install from source

# PlutoSDR support
# pyadi-iio>=0.0.14  # Analog Devices IIO library

# SoapySDR support
# SoapySDR  # System package, install via package manager

# Optional Performance Libraries
numba>=0.56.0  # JIT compilation for performance
psutil>=5.8.0  # System resource monitoring

# Development and Testing (Optional)
pytest>=7.0.0
pytest-qt>=4.0.0
black>=22.0.0
flake8>=4.0.0

# Documentation (Optional)
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
sphinx>=6.2.0
sphinx-rtd-theme>=1.2.0

# Optional dependencies for specific SDR devices
# Uncomment as needed:
# plutosdr>=0.1.0      # For PlutoSDR
# uhd>=3.15.0          # For USRP devices
# hackrf>=2018.1.1     # For HackRF One